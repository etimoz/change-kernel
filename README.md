# Script to change kernel version
A python script to change the kernel version of linux via grub bootloader.

## Requirements
* python 3
* elevated privileges

## Usage
`sudo ./change_linux_kernel.py`